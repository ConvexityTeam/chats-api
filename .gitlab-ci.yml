stages:
  - deploy

before_script:
  - eval $(ssh-agent -s)
  - ssh-add $SSH_PRIVATE_KEY

deploy_staging:
  stage: deploy
  script:
    - ssh ubuntu@$APP_URL -o StrictHostKeyChecking=no
    - eval $(ssh-agent -s)
    - ssh-add $GITHUB_KEY
    - ls
    - cd convexity-blockchain
    - git pull origin master
    - docker-compose build consumer backend
    - docker-compose up -d
    - exit
  only:
    - master
